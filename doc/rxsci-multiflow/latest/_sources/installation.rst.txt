Installation
==============

The releases of RxSci Multiflow are published on `pypi <https://pypi.org>`_. You can
install RxSci Multiflow with pip:

.. code:: console

    python3 -m pip install rxsci-multiflow
